# План на 5 часов — «Граф денег» (HackAlem AI)

**A** — пайплайн (метрики, роли, CSV)
**B** — интерфейс, AI-слой, README

Капитан и финальное слово по объёму работ — капитан команды.

---

## Этап 1. Старт (0:00–0:20)

**Вместе:**
- [ ] Создать репозиторий, дать доступ, положить `data/` и `starter/`
- [ ] Добавить `CLAUDE.md` с контекстом и правилами проекта
- [ ] `requirements.txt`: pandas, pyarrow, networkx, numpy, streamlit, pyvis, pyyaml
- [ ] Запустить стартовый код и убедиться, что три CSV создаются
- [ ] Договориться: A работает в `pipeline/`, B в `app/`, общий контракт — `out/`

**Результат:** оба запустили проект, у каждого своя папка, конфликтов в git нет.

---

## Этап 2. Фундамент (0:20–1:30)

**A:**
- [ ] `features.py`: HITS, betweenness, `n_seed_payers`, `seed_upstream`, `observability` (observed / truncated / no_edges), `fast_pass` по датам, `in_cycle`
- [ ] Сохранить всё в `out/features.parquet`

**B:**
- [ ] Каркас Streamlit на моках в схеме ТЗ
- [ ] Экран «Поиск узла»: ввод gid → ego-граф через pyvis (стрелки, цвет роли, толщина = сумма)
- [ ] Единая легенда цветов ролей

**Чекпоинт 1:30 (5 мин):** A отдаёт `features.parquet`, B подключает реальные данные вместо моков.

---

## Этап 3. Ядро (1:30–2:30)

**A:**
- [ ] `roles.py`: правила по порядку, все пороги в `config.yaml`
- [ ] Для каждого узла `role_score`, `priority_score` (seed ×0.6) и `evidence` с числами
- [ ] `clusters.py`: Louvain (seed=42) + шаблонные гипотезы
- [ ] `run.py`: одна команда → три CSV

**B:**
- [ ] Экран «Топ приоритетов» с переходом на узел
- [ ] Экран «Кластеры»
- [ ] `llm.py`: AI-карточка узла (без ключа кнопка скрыта, приложение работает)

**Чекпоинт 2:30 — главный.** Полный прогон `parquet → CSV → UI` на реальных данных.
- Если роли не готовы, режем до consolidator / distributor / terminal / peripheral
- Если граф тормозит, оставляем только ego-сеть на 2 шага

---

## Этап 4. Проверка и усиление (2:30–3:15)

**A:**
- [ ] `validate.py`: 2 248 строк, нет пустых полей, в топе ≥20 узлов, `cluster_id` у каждого, время прогона
- [ ] Сверить распределение ролей: если в какой-то роли меньше 3 или больше 400 узлов, подкрутить пороги и записать обоснование
- [ ] Выбрать 5 героев для демо и проверить их evidence глазами

**B:**
- [ ] Агент-вопросы по графу (`ask_graph` с tool calling), **только если** этап 3 закрыт полностью
- [ ] Экран «Обзор»: распределение ролей и блок «Ограничения данных»

---

## ⛔ 3:15 — фича-фриз

Дальше никакой новой функциональности, только доводка, документация и демо.

---

## Этап 5. Доводка и документация (3:15–4:00)

**A:**
- [ ] Опционально: устойчивость сети (удаляем топ-N узлов → считаем, на сколько компонент распадается сеть)
- [ ] Прогон на чистом venv строго по README проекта

**B — README проекта (25 баллов, пишется полностью):**
- [ ] Что делает инструмент (2–3 предложения)
- [ ] Запуск одной командой
- [ ] Таблица правил ролей с порогами и обоснованием
- [ ] Как учтены ловушки данных: обрыв на 4-м колене, неполные входящие, seed, порог 5 000 ₸
- [ ] Описание выходных файлов
- [ ] Ограничения подхода
- [ ] Масштабирование до 1 млн узлов
- [ ] Слайд-схема: данные → метрики → роли → интерфейс

---

## Этап 6. Демо (4:00–4:40)

**Вместе:**
- [ ] Два прогона демо по сценарию с секундомером (5 мин)
- [ ] Тренировка: один называет случайный gid, второй за минуту объясняет его роль по метрикам
- [ ] Запись резервного видео демо
- [ ] Финальный коммит, проверка, что `out/` с тремя CSV лежит в репозитории

**Сценарий демо:**
1. Проблема: известен 81 курьер, в сети 2 248 узлов
2. Живой прогон пайплайна
3. Топ-1 `100000008603629100`: не был в списке, у него 19 плательщиков и 61 получатель. Показать ego-граф и AI-карточку
4. Seed `100000005382566100`, в который переводят 6 других seed
5. Ловушка 444 узлов 4-го колена и как мы её отделили
6. Устойчивость сети (если сделали)

---

## Этап 7. Буфер и сдача (4:40–5:00)

Не планировать сюда работу. Это время на непредвиденные поломки и спокойную сдачу.

---

## Правила на весь день

- **Git:** мелкие коммиты, `git pull` перед каждым push, чужую папку не трогать
- **Синхронизации:** 1:30, 2:30, 3:15. На каждую не больше 5 минут
- **Застрял больше 15 минут** — говоришь напарнику и упрощаешь решение
- **Приоритет задач:** работающий пайплайн → объяснимые роли → README → визуализация → AI → опциональные пункты
- **Формулировки везде осторожные:** «признаки консолидации», а не «организатор»
