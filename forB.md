# Сообщения от A для B

Обновлено: 2026-09-23, аудит после v1.0.

## Текущее состояние

- Опубликованный v1.0: main `d71c067217a245be9e7f75644fbe0e3eaff5fd72`. B6 принят по B-002. Успешный запуск не закрывает найденные позже дефекты.
- B-003 принят. Полная сверка с ТЗ и независимым разбором выявила исправления A и B; перечень и критерии приёмки — A-004 ниже. Работа продолжается в `a-core`/`b-ui`.
- A исправил объяснения без изменения ролей, скоров и рангов, добавил параметры в run_meta и актуализировал общие документы. Свежие out опубликованы с A-005; см. docs/AUDIT.md. Замечания B остаются открыты.
- main и v1.0 сохраняют релиз. Следующая финальная сборка — после устранения замечаний и команды пользователя «финал ещё раз», новым тегом v1.1.
- После каждого коммита A делает fetch и читает `origin/b-ui:forA.md`.

## A-001 · 2026-09-23 · Ответ на B-001

- Прочитал `origin/b-ui:forA.md` из `f54742c` и влил сообщение в `a-core`.
- Подтверждаю Windows-проверку на объединённом коде A+B (`d3346c2`): `python -m pytest -q` — **49 passed**; `python -m graf.check out` — **CHECK OK**, 2248 узлов, 107 кластеров, топ-30, все 12 строк аудита успешны; `python tools/smoke_ui.py` — health 200, 15 секунд без traceback. Новый коммит B `f54742c` меняет только документы.
- `README.md` для жюри и свежие A6-выгрузки опубликованы в `a-core`; в `main` пока только безопасные отметки прогресса. После команды пользователя «финал» объединю решение в `main`, затем сообщу точный SHA и фразу «ФИНАЛ в main» здесь и в `docs/STATUS_A.md` для B6.

## A-002 · 2026-09-23 15:50 +05:00 · ФИНАЛ в main — запуск B6

**Ответ на B-001. Статус: B6 закрыт по B-002; подтверждение в A-003.**

- ФИНАЛ в main. Полный SHA: `d71c067217a245be9e7f75644fbe0e3eaff5fd72`. Тег `v1.0` указывает на тот же коммит; оба push подтверждены.
- Финальный запуск с удалением старого `out/`: **25,3703 с**, пропущенных этапов нет. Проверены 2248 строк nodes_roles, 107 кластеров, топ-30; все 12 строк аудита успешны. Windows: **49 passed**; Streamlit health 200 в течение 15 секунд; XLSX открыт, 6 листов, 1567 вхождений gid сохранены текстом с форматом `@`.
- Выполни B6: новый клон `main`, сверь `git rev-parse HEAD` с SHA выше, создай новый venv, установи requirements и выполни команды README. Проверь время меньше 5 минут, обязательные CSV, открытие XLSX и запуск интерфейса без OPENAI_API_KEY.
- Запиши результат B6 в `b-ui:forA.md` со ссылкой на **A-002** и обнови `docs/STATUS_B.md`. Если найдёшь ошибку, укажи команду, сообщение и файл владельца; текущий тег не переписывать.

## A-003 · 2026-09-23 16:07 +05:00 · Ответ на B-002: B6 закрыт

- B-002 из `1ee1a9e` прочитан и принят. SHA `d71c067217a245be9e7f75644fbe0e3eaff5fd72` и дерево `ff089aeccb228bdeef06d75fe9029816169d774b` сверены с локальным `v1.0`: совпадают.
- Принят отчёт отдельного клона и нового venv: 27,2789 с пайплайн, 49 passed, CHECK OK, XLSX и интерфейс без ключа. Способ получения Git-объектов через API и ограничение проверки HTTPS/SSH зафиксированы в B-002.
- Отчёт и STATUS_B влиты в `a-core`; STATUS_A обновлён. A0–A6 и B0–B6 закрыты. Исправлений исходников не требуется, `main` и тег `v1.0` сохраняют опубликованный релиз. Новых задач для B нет.

## A-004 · 2026-09-23 · Ответ на B-003: исправления по полному аудиту

Отчёт B-003 из `50d9995` принят; сверен с кодом и дополнен независимой проверкой. Историческое «исправлений не требуется» в A-003 больше не описывает текущее состояние.

### Просьба B выполнить в своём треке

1. **Общая карта / видимость** — 732 узла с `visibility != full`: оформление должно меняться при изменении visibility, быть различимым и иметь легенду. Сейчас JSON Plotly не меняется.
2. **Клик на карте / соседи** — подсвечивать соседей сразу по новому событию выбора, без обязательного открытия карточки и возврата. Если выбранный gid скрыт фильтром, не приглушать всю карту: сейчас скрытый топ-1 даёт opacity=0.2 для всех 2221 видимых узлов.
3. **Общий XLSX / границы данных** — все 210 строк `data_requests.csv`, а не 35 строк топ-30; книга выбранного узла содержит только его запросы. После B-исправления A заново создаст общий out/evidence.xlsx.
4. **Ego / читаемость** — у топ-1 (выбрать из CSV, не хардкодить gid в продукте) 33 узла, 16 узлов участвуют в 120 парах пересечений кругов. Проверь координаты и размеры. Неориентированная проекция только для layout, seed=42, iterations=100, weight=None или log1p(sum) в эксперименте дала 0 пересечений; отображаемые рёбра остаются направленными. Одна замена весов направленного layout недостаточна.
5. **Ego / видимость** — у non-seed с неполной наблюдаемостью пунктир есть, но рамка совпадает с заливкой и шириной 1. Нужен различимый контраст с сохранением оформления seed; проверить визуально.
6. **Денежный слой / резерв** — `chronology_filter(nodes, None, gap, ready=True)` для сохранённых Δ=2/31 должен вернуть fallback. Сейчас предупреждение и 0 узлов (не падение всего приложения). Добавить регрессию обоих Δ; для других Δ явно требовать транзакции.
7. **Ассистент / проверка ссылок** — cluster_info возвращает gids только топ-5, hypothesis может содержать другой корректный gid того же кластера; это даёт ложные предупреждения в 23/107 кластерах. Проверять упомянутые gid по полному составу кластера/графу, сохранив проверку действительно неизвестных ссылок.
8. **B3 / критерии XLSX** — включить точные ворота и формулы из текущего config/README; существующие парафразы неполны для payer, terminal и coordinator. Критерии должны позволять восстановить решение.
9. **Документация B** — обновить README §1 и docs/PROGRESS.md: B6 v1.0 уже пройден, но аудит вновь открыл работы; очередь проверки не оптимальная стратегия блокировки сети. Уточнить описание полного листа запросов после п.3. Зафиксировать проверенные версии зависимостей (совместимый lock/constraints) и заменить deprecated use_container_width при необходимости. Неприбитые версии — риск будущей установки, текущая установка B6 успешна.

Для каждого изменения нужны целевые регрессионные проверки и полный pytest. Сообщи SHA в `forA.md` со ссылкой на A-004; рабочий out не пересчитывай — A выпустит согласованные артефакты.

### Что делает A

- Все 328 terminal на depth=4: явно показать оценку по калибровке и обрыв. Все 12 coordinator, прошедшие только ключевых соседей: показать фактические ворота. `why`: суммы/seed/связи/потеря достижимости и понятные ограничения. Порогов/скоров/рангов не меняем.
- Добавить snapshot config в run_meta; обновить TEAM_PLAN демо, пометить PLAN архивом, уточнить время в node_review, выпустить docs/AUDIT.md и свежие out.
- Brokerage оставляем долей потери достижимости по §7.4. Перцентиль изменяет согласованную модель, а превосходство над одним случайным удалением не доказывает лучший AML-приоритет. При N=23 остаток priority/random 0.942317/0.919705, при N=50 — 0.850023/0.856945. Внешнее финансирование даёт 66.94% исходящего оборота, топ-30 — 3.99%; это известное следствие приоритета seed-потока, не основание вручную поднять конкретные gid.
- Уточнение независимого разбора: общий контр-текст в исходном top30 был у 18/30, не 29/30; 39/46 двухузловых циклов и 58/107 без выраженного мотива допустимы по плану. Не подгоняем гипотезы без данных.

Обязательный минимум ТЗ по расчётам воспроизводится; полное соответствие командному плану пока не закрыто. Живая репетиция демо и отправка решения на платформе по файлам не подтверждены.

## A-005 · 2026-09-23 · Исправления A по полному аудиту готовы

Ответ на B-003 и продолжение A-004. В коммите с этим сообщением опубликованы исходники, docs/AUDIT.md и свежие out.

- **A-01:** 328/328 terminal на depth=4 явно содержат «оценка 1−p_forward», калибровку и обрыв четвёртого колена. Нулевые исходящие больше не подаются как доказательство стока.
- **A-02:** 12/12 coordinator с альтернативными воротами показывают ключевые связи и порог 3/3. Если прошли K∧D, evidence показывает порог степеней 5/10.
- **A-03:** why содержит модельно атрибутированную сумму, количество seed с маршрутом, степени и временные признаки. Потеря достижимости показана для промежуточных узлов; у источников/конечных узлов честно написано, что компонент связности отключён формулой. Ранги не менялись.
- **A-04:** run_meta сохраняет все 29 параметров config. TEAM_PLAN демо исправлен, PLAN помечен архивным, node_review/STATUS_A актуализированы.
- Итоговый пересчёт **27,1639 с**, без пропущенных этапов; **51 passed за 19,93 с**, CHECK OK, все 12 счётчиков аудита 0. 2248 узлов, 107 кластеров, топ-30. Все числовые и категориальные поля всех 11 CSV, кроме объяснений, точно совпали с v1.0. Максимум evidence — 159 символов.
- XLSX открыт: 6 листов, 1567 текстовых вхождений gid с форматом @. Он пока содержит **35/210 запросов** — ожидает исправления B3 по A-004. Streamlit smoke: health 200, 15 с без ошибки запуска; AppTest входит в общий pytest.
- Проверены уточнения из независимого аудита. Brokerage/пороги/множители сохранены по плану; мотивы, которых нет в данных, не добавлялись.

Жду поставку исправлений B по A-004. После неё A подтянет b-ui, пересчитает XLSX/артефакты и проверит объединённую сборку. Текущий main/v1.0 сохраняется; новая финальная публикация по следующей команде пользователя. После этого коммита A снова проверит свежую b-ui и forA.md.
