# STATUS A — аналитическое ядро

Рабочая ветка: `a-core`. Полная сборка с B запланирована на ФИНАЛ.

| Задача | Статус | Коммит |
|---|---|---|
| A0 | готово | `dbc69dc` |
| A1 | готово | `eb90666` |
| СТРАХОВКА | в работе | A1 даёт три валидных CSV; публикация в `main` ранее дважды отклонена автоматической проверкой |
| A2 | готово | `111f212` |
| A3 | готово | этот коммит (см. `git log`) |
| A4 | в работе | — |
| A5 | не начато | — |
| A6 | не начато | — |
| ФИНАЛ | не начато | ждём `B-ready-for-final` и команды «финал» |

## Готово для B

- `graf.flow.chrono_reach(tx, seeds, gap_days, max_hops=4)` готов с A2 для ползунка Δ; `out/paths.csv`, `out/ablation_links.csv`, `out/tracked_by_depth.csv` содержат реальные расчёты.
- `out/nodes_roles.csv` после A3 содержит реальные роли, ворота, скоры, флаги, сигналы и evidence для 2248 узлов. `out/clusters.csv` и `out/top_nodes.csv` пока имеют временную кластеризацию и приоритет до A4.
- `out/graph.json` содержит строковые gid и `tracked_kzt` на рёбрах. `out/evidence.xlsx` создан кодом B на текущих данных.

## Изменения контракта

Предусмотренные разделом 4 дополнительные поля и файлы A2–A3; обязательные колонки не удалены и не переименованы.

## Запросы к B

- Опубликовать `docs/STATUS_B.md` с запросами к A и отметкой `B-ready-for-final`, когда B будет готов.
- В `a-core` при слиянии сохранены реализации B для `app.py`, `graf/evidence_xlsx.py`, `graf/assistant.py`, `ui/__init__.py`.

## Решения, которые я принял сам

- Для A2 учёт переводов внутри одного дня сортируется по глубине отправителя, затем по gid; транзакции имеют только даты.
- Пути в `paths.csv` выбираются по максимальной минимальной сумме на маршруте, затем по меньшему числу шагов; до A4 использовался временный топ-30.
- `docs/CODEX_A.md` и `docs/CODEX_B.md` появились локально без истории Git; не редактирую и не добавляю их в свой коммит.
- Перед A3 в `a-core` влиты свежие коммиты `origin/b-ui` (`603ce47`) по владению файлами; конфликтов не было.

## Блокеры

Нет блокеров для A4–A6. `docs/STATUS_B.md` и отметка `B-ready-for-final` в `origin/b-ui` пока не опубликованы; ФИНАЛ не готов.
