# STATUS A — аналитическое ядро

ФИНАЛ в main, тег `v1.0`. Точный SHA main и коммита тега: `d71c067217a245be9e7f75644fbe0e3eaff5fd72`. Push main и v1.0 подтверждены. Общая сборка содержит A0–A6 и B0–B5. Связь с B: `a-core:forB.md`, сообщение A-002; B6 пройден по отчёту B-002 (`1ee1a9e`), подтверждён сообщением A-003. Все задачи A0–A6 и B0–B6 закрыты.

| Задача | Статус | Коммит |
|---|---|---|
| A0 | готово | `dbc69dc` |
| A1 | готово | `eb90666` |
| СТРАХОВКА | заменено | пользователь отложил финансовые выгрузки в main до финала; опубликована безопасная отметка `623e477` |
| A2 | готово | `111f212` |
| A3 | готово | `d1d07f4` |
| A4 | готово | `a88dc21` |
| A5 | готово | `02621e5` |
| A6 | готово | `0abb8a0`; повторный чистый пересчёт в финальном релизе |
| ФИНАЛ | готово | `d71c067` в `main`, тег `v1.0`; B6 пройден (B-002) |

## Готово для B

- graf.flow.chrono_reach готов. Сигнатура: `chrono_reach(tx, seeds, gap_days, max_hops=4)`; полный расчёт занимает менее 1 с и подходит для ползунка Δ.
- `out/nodes_roles.csv`: 2248 реальных ролей с воротами, скорами, видимостью, сигналами и числовым evidence. `out/top_nodes.csv`: финальный топ-30 без payer/excluded. `out/clusters.csv`: 107 сообществ Louvain с гипотезами.
- `out/paths.csv`, `out/seeds_review.csv`, `out/resilience.csv`, `out/ablation_links.csv`, `out/tracked_by_depth.csv`, `out/data_requests.csv`, `out/audit.csv`, `out/truncation_calibration.csv` — реальные выгрузки A2–A6. `out/graph.json` содержит строковые gid, числовые дни и `tracked_kzt` на рёбрах.
- A6 пересчитан с нуля из `.venv` за 25,37 с без пропущенных этапов. Обязательные CSV/JSON проверены; `evidence.xlsx` заново создан, открыт, содержит 6 листов и текстовые 18-значные gid (`@`). `pytest -q`: 49 passed; `tools/smoke_ui.py`: health 200, 15 с без traceback. Аудит top10/top30: все счётчики ловушек 0.
- `docs/node_review.md`: топ-10 и три демо-узла с воротами, скорами ролей, приоритетом и контр-сигналами; пороги A5 менять не потребовалось.

## Изменения контракта

Предусмотренные разделом 4 дополнительные поля и файлы A2–A6; обязательные колонки не удалены и не переименованы. Дни `first_day`/`last_day` в `graph.json` — числа 1–31.

## Запросы к B

- B5 README, схема, скриншоты и исправления Windows-тестов влиты в `a-core`; общий тестовый набор теперь зелёный. README заполнен подтверждёнными числами из A6 по прямому запросу пользователя.
- Получен B-002 из `1ee1a9e`: B0–B6 готовы. По отчёту B проверен точный SHA релиза в отдельном клоне и новом venv Python 3.12.14: пайплайн 27,2789 с, команда с установкой 71,03 с, 49 passed, CHECK OK, XLSX и интерфейс без API-ключа. Git-объекты получены через API с проверкой SHA; ограничение HTTPS/SSH описано в `forA.md`. Дополнительных исправлений не требуется.
- Подтверждённой развёрнутой версии нет; ссылку на неё в README не добавляю.

## Решения, которые я принял сам

- Для A2 учёт переводов внутри одного дня сортируется по глубине отправителя, затем по gid; транзакции имеют только даты.
- `paths.csv` выбирает пути по максимальной минимальной сумме, затем по меньшему числу шагов; после A4 пересчитан для финального топ-30.
- Louvain применяется внутри слабых компонент к неориентированной проекции с весом `sum_kzt`; размер сообщества определяет порядок `cluster_id`.
- В A5 потеря seed-достижимости при удалении узла включает сам удалённый узел, как буквально задано в 7.4. Порядок топ-30 не изменился.
- `run.py` сохраняет обязательные CSV при ошибке создания XLSX, но отмечает этап пропущенным и удаляет старый XLSX, чтобы не выдавать его за свежий.
- `docs/CODEX_A.md` и `docs/CODEX_B.md` появились локально без истории Git; не редактирую и не добавляю их в свой коммит.

## Блокеры

Блокеров нет. Финальный пересчёт и 49 тестов на Windows завершены; B6 на отдельном клоне и новом окружении принят по B-002. SHA коммита и дерева B совпадают с опубликованным `v1.0`.
