# STATUS A — аналитическое ядро

Рабочая ветка: `a-core`. Полная сборка с B запланирована на ФИНАЛ.

| Задача | Статус | Коммит |
|---|---|---|
| A0 | готово | `dbc69dc` |
| A1 | готово | `eb90666` |
| СТРАХОВКА | в работе | A1 даёт три валидных CSV; публикация в `main` ранее дважды отклонена автоматической проверкой |
| A2 | готово | `111f212` |
| A3 | готово | `d1d07f4` |
| A4 | готово | этот коммит (см. `git log`) |
| A5 | в работе | — |
| A6 | не начато | — |
| ФИНАЛ | не начато | ждём `B-ready-for-final` и команды «финал» |

## Готово для B

- `graf.flow.chrono_reach(tx, seeds, gap_days, max_hops=4)` готов с A2 для ползунка Δ; функция на полных данных работает менее 1 с.
- `out/nodes_roles.csv`: 2248 реальных ролей с воротами, скорами, видимостью, сигналами и числовым evidence. `out/top_nodes.csv`: финальный топ-30 без payer/excluded. `out/clusters.csv`: 107 сообществ Louvain с гипотезами.
- `out/paths.csv`, `out/seeds_review.csv`, `out/resilience.csv`, `out/ablation_links.csv`, `out/tracked_by_depth.csv`, `out/data_requests.csv`, `out/audit.csv`, `out/truncation_calibration.csv` — реальные выгрузки A2–A4. `out/graph.json` содержит строковые gid и `tracked_kzt` на рёбрах.
- `out/evidence.xlsx` создаётся реализацией B на текущих данных. Полный запуск из `.venv` прошёл за 24,63 с; 17 тестов ядра прошли. Аудит top10/top30: все счётчики ловушек 0.

## Изменения контракта

Предусмотренные разделом 4 дополнительные поля и файлы A2–A4; обязательные колонки не удалены и не переименованы.

## Запросы к B

- В `origin/b-ui:docs/STATUS_B.md` от B2 запрошены статус A и файлы paths, seeds_review, resilience, ablation_links, tracked_by_depth, data_requests, audit. Всё перечисленное опубликовано в этом коммите A4.
- Опубликовать отметку `B-ready-for-final`, когда B3–B6 будут готовы. Файлы B в `a-core` не редактирую.

## Решения, которые я принял сам

- Для A2 учёт переводов внутри одного дня сортируется по глубине отправителя, затем по gid; транзакции имеют только даты.
- `paths.csv` выбирает пути по максимальной минимальной сумме, затем по меньшему числу шагов. После A4 он пересчитан для финального топ-30.
- Louvain применяется внутри слабых компонент к неориентированной проекции с весом `sum_kzt`; размер сообщества определяет порядок `cluster_id`.
- Перед A3/B2 в `a-core` влиты свежие коммиты `origin/b-ui` (`603ce47`, `cf0311b`) по владению файлами; конфликтов не было.
- `docs/CODEX_A.md` и `docs/CODEX_B.md` появились локально без истории Git; не редактирую и не добавляю их в свой коммит.

## Блокеры

Нет блокеров для A5–A6. Отметки `B-ready-for-final` в `origin/b-ui` пока нет; ФИНАЛ не готов.
