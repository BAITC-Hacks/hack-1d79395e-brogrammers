# STATUS A — аналитическое ядро

Рабочая ветка: `a-core`. `main` содержит A0; полная сборка с B запланирована на ФИНАЛ.

| Задача | Статус | Коммит |
|---|---|---|
| A0 | готово | `dbc69dc` |
| A1 | готово | `eb90666` |
| СТРАХОВКА | в работе | A1 уже даёт три валидных CSV; публикация в `main` ранее дважды отклонена автоматической проверкой, новое явное поручение получено |
| A2 | готово | этот коммит (см. `git log`) |
| A3 | в работе | — |
| A4 | не начато | — |
| A5 | не начато | — |
| A6 | не начато | — |
| ФИНАЛ | не начато | ждём `B-ready-for-final` и команды «финал» |

## Готово для B

- `out/nodes_roles.csv`, `out/clusters.csv`, `out/top_nodes.csv`, `out/graph.json` — реальные графовые выгрузки A1; роли и приоритет пока временные.
- `graf.flow.chrono_reach(tx, seeds, gap_days, max_hops=4)` — готов A2, возвращает число seed для каждого достижимого gid; годится для ползунка Δ.
- `out/paths.csv`, `out/ablation_links.csv`, `out/tracked_by_depth.csv` — реальные выгрузки A2. В `graph.json` у рёбер есть `tracked_kzt`.

## Изменения контракта

Только предусмотренные разделом 4 дополнительные поля и файлы A2; обязательные колонки не удалены и не переименованы.

## Запросы к B

- Опубликовать `docs/STATUS_B.md`, когда появятся запросы к A и отметка `B-ready-for-final`.
- При сборке брать реализацию B для `app.py`, `graf/evidence_xlsx.py`, `graf/assistant.py`, `ui/__init__.py`: в A0 там заглушки.

## Решения, которые я принял сам

- Для A2 учёт переводов внутри одного дня сортируется по глубине отправителя, затем по gid; транзакции имеют только даты.
- Пути в `paths.csv` выбираются по максимальной минимальной сумме на маршруте, затем по меньшему числу шагов; для временного топ-30 до A4.
- `docs/CODEX_A.md` и `docs/CODEX_B.md` появились локально без истории Git; не редактирую и не добавляю их в свой коммит.

## Блокеры

Нет блокеров для A3–A6. Статус B для финала пока не опубликован.
