# Промты участника A

## A0 — скелет

> Задача A0. starter.py теперь в корне репозитория, данные — в data/, полное ТЗ — в docs/TZ.md, README стартера — в docs/STARTER_README.md.
> Выполни A0 строго по docs/TEAM_PLAN.md (разделы 2–5 и 7.1): скелет, graf/load.py из starter.py, config.py, заглушки файлов трека B,
> run.py, который уже сейчас пишет три обязательных CSV, requirements.txt, .gitignore, docs/codex_prompts_A.md.
> Проверь запуск: python run.py --data data --out out. Затем закоммить "chore: skeleton [codex]", запушь в main,
> создай ветку a-core и запушь её. После этого сразу переходи к A1.

Рабочий план: `docs/TEAM_PLAN.md`, задача A0 в разделе 7.5.

## A1 — признаки и первые выгрузки

> После этого сразу переходи к A1.

Источник задания: `docs/TEAM_PLAN.md`, раздел 7.5, A1. Реализовать `graf/features.py`, `graf/temporal.py`, `graf/truncation.py`, временные роли и выгрузки по контракту 4.1–4.4, `graf/check.py` и `tests/test_core_features.py`; проверить полный запуск менее 60 секунд.

## A2 — деньги курьеров

> Запушь сам и продолжай.
> Сравнивай с b-ui: там работает мой напарник; делай коммит пока в a-core, в самом конце объединим в main общее решение.

Источник задания: `docs/TEAM_PLAN.md`, раздел 7.5, A2. Реализовать `chrono_reach`, топологическую и хронологическую экспозицию seed, FlowLedger с ограничением доступного баланса, до трёх датированных путей к узлам топ-30 и таблицу абляции; сверить контракт `paths.csv` и `chrono_reach` с `origin/b-ui`.

## Обновлённое поручение: автономно A0–A6 и Git-протокол

Я участник A (трек A — аналитическое ядро). Работай автономно по плану до конца, не спрашивай меня после каждого шага.

=== КОНТЕКСТ ===
- Прочитай AGENTS.md и docs/TEAM_PLAN.md целиком: мой трек — раздел 7, контракт между треками — раздел 4, владение файлами — раздел 3, таймлайн — раздел 6, что резать — раздел 10. Полное ТЗ — docs/TZ.md, стартер — starter.py, данные — data/.
- Напарник (участник B) параллельно делает трек B (раздел 8) на своём компьютере в ветке b-ui. Я работаю в ветке a-core. Общее решение попадает в main в самом конце.
- Окружение: .venv (если нет — python -m venv .venv и pip install -r requirements.txt). Все команды — из .venv.

=== GIT-ПРОТОКОЛ (выполняй сам) ===
1. Работаю только в a-core: git checkout a-core. Если ветки нет — git checkout -b a-core от main и git push -u origin a-core.
2. Перед каждой задачей:
   - git fetch origin;
   - прочитай статус напарника: git show origin/b-ui:docs/STATUS_B.md (если ветки или файла нет — пропусти);
   - если в origin/b-ui есть новые коммиты (git log --oneline HEAD..origin/b-ui), влей их: git merge --no-edit origin/b-ui.
   Сливай только при чистом рабочем дереве: сначала закоммить свою задачу.
3. Конфликты решай сам по владению файлами (раздел 3):
   - файлы трека B — версия B: git checkout --theirs <файл>;
   - файлы трека A и out/ — моя: git checkout --ours <файл>;
   - общие (requirements.txt, .gitignore, AGENTS.md, docs/TEAM_PLAN.md) — объедини обе стороны вручную;
   - затем git add и git commit --no-edit.
   НИКОГДА: push --force, rebase опубликованных веток, reset --hard, удаление веток.
4. После каждой задачи:
   - pytest -q tests/test_core_*.py;
   - python run.py --data data --out out;
   - обнови docs/STATUS_A.md;
   - git add только файлы трека A + out/ + docs/STATUS_A.md + docs/codex_prompts_A.md + docs/node_review.md;
   - git commit -m "<тип>(<модуль>): <что> [codex]";
   - git push origin a-core.
   Если push не проходит из-за авторизации — продолжай коммитить локально и в конце напиши мне «запушь a-core через GitHub Desktop».
5. Файлы трека B не редактируй, даже если видишь в них ошибку: запиши её в «Запросы к B» своего STATUS_A.md. Запросы B ко мне лежат в разделе «Запросы к A» файла STATUS_B.md. Выполняй их, если они не ломают контракт.
6. Каждый свой промт и задачу записывай в docs/codex_prompts_A.md.

=== docs/STATUS_A.md (ведёшь сам, обновляешь после каждой задачи) ===
- Таблица задач A0–A6 + СТРАХОВКА + ФИНАЛ: статус (готово / в работе / не начато), хеш коммита.
- «Готово для B»: какие файлы out/ и функции уже реальные. Например: «out/nodes_roles.csv — реальные роли (A3)», «graf.flow.chrono_reach готов (A2)».
- «Изменения контракта»: только добавления колонок. Ничего не удаляй и не переименовывай.
- «Запросы к B», «Решения, которые я принял сам», «Блокеры».

=== ПОРЯДОК РАБОТЫ ===
Сначала по git log и docs/STATUS_A.md определи, что уже сделано, и продолжай с первой невыполненной задачи:
A0 → A1 → СТРАХОВКА → A2 → A3 → A4 → A5 → A6 → ФИНАЛ.
Промты и критерии готовности — TEAM_PLAN, раздел 7.5. Константы — 7.1, роли и флаги — 7.2–7.3, приоритет — 7.4. Ориентируйся на таймлайн раздела 6; если отстаёшь — режь по разделу 10. Сначала рабочий минимум каждой задачи, полировка — только после A4.

Уточнения к задачам:
- A0: .gitignore должен содержать .venv/, __pycache__/, *.pyc, .env, out_stub/, out_local/. Папку out/ НЕ игнорировать: её коммитит только A.
- run.py: этап evidence_xlsx оберни в try/except Exception. Ошибка или NotImplementedError в коде B не должна ломать обязательные выгрузки: печатай предупреждение и продолжай.
- СТРАХОВКА (сразу после A1, когда run.py пишет 3 валидных CSV и check зелёный):
  git checkout main && git pull && git merge --no-edit a-core && git push origin main && git checkout a-core.
  Отметь в STATUS «СТРАХОВКА в main». До ФИНАЛА в main больше ничего не пиши.
- A2: после готовности отметь в STATUS «graf.flow.chrono_reach готов» — B ждёт это для ползунка Δ.
- A5 выполняй сам. Выведи в docs/node_review.md (файл трека A) карточки для демо:
  - топ-10;
  - 3 узла для демо: топ-1; лучший consolidator с высоким tracked_share_in; узел с ролью coordinator и контр-сигналом.
  В карточке: ворота с порогами, скоры всех ролей, компоненты приоритета, контр-сигналы. Проверь аудит ловушек. Пороги правь только в config.py, обоснование — в STATUS. gid в коде не хардкодить.

=== ФИНАЛ ===
Условие: A6 готов и в STATUS_B.md (origin/b-ui) есть отметка «B-ready-for-final». Если отметки нет — закончи всё остальное, запиши в STATUS «жду B» и остановись. Когда я напишу «финал» — продолжи с этого места.
1. git fetch origin && git merge --no-edit origin/b-ui (конфликты — по протоколу).
2. Удали out/ и запусти с нуля python run.py --data data --out out. Проверь:
   - 3 CSV + evidence.xlsx;
   - check и аудит зелёные, время < 5 мин;
   - pytest -q (все тесты);
   - streamlit run app.py --server.headless true стартует без исключений: запусти в фоне на ~15 с, проверь лог, останови.
3. git add -A (кроме игнорируемого) → git commit -m "release: final outputs [codex]" → git push origin a-core.
4. git checkout main && git pull && git merge --no-edit a-core && git push origin main && git tag v1.0 && git push origin v1.0 && git checkout a-core.
5. В STATUS_A.md: «ФИНАЛ в main, тег v1.0». Закоммить, запушь a-core. Напиши мне итог: что в main, время пайплайна, таблица аудита, что не успели.
Если после финала B прислал исправления (в STATUS_B «исправления после финала») и я напишу «финал ещё раз» — повтори шаги 1–5, тег v1.1.

=== КОГДА ОСТАНАВЛИВАТЬСЯ И СПРАШИВАТЬ МЕНЯ ===
Только если:
- в data/ нет parquet-файлов;
- git не даёт работать даже локально;
- конфликт нельзя решить по правилу владения;
- требование ТЗ противоречит плану.
Во всех остальных случаях решай сам и записывай решение в STATUS_A.md.
Если ты прервался — по слову «продолжай» прочитай docs/STATUS_A.md и git log и продолжи с первой невыполненной задачи.
## Уточнение по сроку

> работай усерднее и быстрее, нужно успеть закончить всё за 3 часа

## A3 — роли и свежие выгрузки

> codex участника Б попросил тебя публиковать docs/Status_A.md и свежие выгрузки в a-core, особенно готовность chrono_reach и финала

Источник критериев: docs/TEAM_PLAN.md, разделы 7.2–7.3 и 7.5 A3. Выполнить роли, флаги, evidence, тесты и полный запуск из .venv; отметить состояние контракта и финала в docs/STATUS_A.md.

## A4 — кластеры, приоритет, устойчивость и аудит

Источник: docs/TEAM_PLAN.md, разделы 4.1–4.4 и 7.4–7.5 A4; выполнить Louvain, отпечатки, объяснимый приоритет, удаление узлов, полный контракт out/ и аудит top10/top30. Выполнить 17 тестов ядра и полный запуск из .venv, обновить STATUS_A.md и опубликовать a-core.

## A5 — проверка демо и уточнения формулы

> Оставить main до финала (рекомендуется)

По docs/TEAM_PLAN.md §7.5 A5 подготовить карточки топ-10 и трёх выбранных по данным gid; пересчитать ворота, скоры, приоритет и аудит. Исправить буквальный учёт потери достижимости, числовые дни graph.json, учитывать сбой evidence.xlsx без остановки обязательных CSV. Порогов не менять без оснований. Записать запросы к B в STATUS_A.md.

## A6 — чистый пересчёт и запрос на часовое подтверждение

> что нибудь запушь в main так как нам требуется каждый час подтверждать что делаем что то
> Оформите README: что делает проект, что реализовано, технологии и архитектура, установка и запуск, пример проверки, данные и внешние сервисы, ограничения, ссылка на развёрнутую версию при наличии. Использовать только проверяемые сведения.

A6: удалить только out/ в рабочем репозитории, запустить пайплайн заново из .venv, проверить CSV/JSON/XLSX, аудит и все тесты. README по разделу 3 принадлежит B; проверить его B5 и опубликовать безопасную отметку прогресса в main без финансовых выгрузок.

## A6 — завершение после B5

После слияния B5 выполнить общий pytest -q, чистый A6 расчёт out/, проверку 6 листов evidence.xlsx, gid-текста Excel и 15-секундный Streamlit smoke. По прямому запросу пользователя оформить README: заполнить B5-шаблон штатным инструментом только фактами текущего репозитория и пройти чек-лист организаторов. Обновить STATUS_A.md; релиз в main только после отметки B-ready-for-final и команды «финал».

## ФИНАЛ — команда пользователя и связь с B

> финал

Выполнить финальный раздел пользовательского задания: свежая b-ui, чистый пересчёт out/, check, общий pytest, проверка запуска Streamlit, коммит и push a-core, объединение и push main, тег v1.0. После публикации передать B точный SHA main для B6 через forB.md и STATUS_A.md. По дополнительному поручению пользователя после каждого коммита обновлять b-ui и читать её forA.md; отвечать на сообщения с их ID. Локальные сторонние docs/CODEX_A.md и docs/CODEX_B.md не включать в релиз.

## Приёмка B6

> проверь forA.md

Обновить origin, прочитать forA.md и STATUS_B из b-ui. Проверить SHA релиза и дерева из B-002, включить отчёт B6 в a-core, закрыть ожидание в STATUS_A и ответить A-003 через forB.md. После коммита повторно проверить b-ui и forA.md. Изменений кода в отчёте нет, повторный прогон и новый релиз не требуются.


## Полный аудит после v1.0

> ты лучше проанализируй проект и сделай полный аудит учитывая замечания агента Б, чтобы он соответствовал требованиям хакатона и задания
> и еще прочитай подробный аудит от агента Б, в файле forA.md

Проверить обязательные требования TZ и TEAM_PLAN, B-003 из b-ui и приложенный независимый аудит; отличать подтверждённые дефекты от гипотез об улучшении модели. Исправить A-объяснения depth4/coordinator/why и параметры run_meta без изменения скоров/рангов. Передать подтверждённые B-замечания через forB.md, обновить документы и свежие out, выполнить полный pytest, pipeline, smoke и сравнение числовых данных с v1.0. Публиковать a-core; main и v1.0 сохраняются до следующей команды финала.


## Полная доработка функциональности и визуальной понятности

> я бы хотел чтобы всё работало по плану и заданию, так же чтобы визуально максимально понятно было
> Доработай весь проект здесь

Пользователь разрешил A редактировать весь проект, включая прежний трек B. Закрыть подтверждённые дефекты аудита, улучшить ясность основного сценария аналитика, проверить живой интерфейс, тесты, XLSX, расчёт и воспроизводимость. Сообщить B о передаче через A-006. Публикация в a-core; финал main по ранее согласованному порядку.

## Закрытие функционального аудита и удобство сценария

> Продумай интуитивно понятный интерфейс, максимально понятный; прочитай отчёт-инструкцию агента Б в forA.md.

Прочитан B-006. Реализованы XLSX непосредственно в карточке, переходы к контрагентам и возврат, сохранение контекста и прозрачные фильтры. Независимый аудит проверен по текущему коду: исправлены объяснения решающих ворот и неполного баланса, случайный сценарий усреднён по 100 прогонам. Формулы приоритета и ранги сохранены. Проверки: 105 тестов, полный расчёт, smoke и основной сценарий в браузере. Пользователь затем поручил обновление визуального оформления, сохраняя требования данных и проверенные функции.
