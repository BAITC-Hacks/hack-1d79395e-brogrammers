# Промты участника A

## A0 — скелет

> Задача A0. starter.py теперь в корне репозитория, данные — в data/, полное ТЗ — в docs/TZ.md, README стартера — в docs/STARTER_README.md.
> Выполни A0 строго по docs/TEAM_PLAN.md (разделы 2–5 и 7.1): скелет, graf/load.py из starter.py, config.py, заглушки файлов трека B,
> run.py, который уже сейчас пишет три обязательных CSV, requirements.txt, .gitignore, docs/codex_prompts_A.md.
> Проверь запуск: python run.py --data data --out out. Затем закоммить "chore: skeleton [codex]", запушь в main,
> создай ветку a-core и запушь её. После этого сразу переходи к A1.

Рабочий план: `docs/TEAM_PLAN.md`, задача A0 в разделе 7.5.

## A1 — признаки и первые выгрузки

> После этого сразу переходи к A1.

Источник задания: `docs/TEAM_PLAN.md`, раздел 7.5, A1. Реализовать `graf/features.py`, `graf/temporal.py`, `graf/truncation.py`, временные роли и выгрузки по контракту 4.1–4.4, `graf/check.py` и `tests/test_core_features.py`; проверить полный запуск менее 60 секунд.
