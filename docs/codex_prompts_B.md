# Работа участника B с Codex

## 2026-09-23 — исходное поручение

> Я участник Б, начинаем работу по плану участника Б (документы в папке).

Прочитаны `AGENTS.md` и `docs/TEAM_PLAN.md` целиком. Актуален TEAM_PLAN, а не ранний PLAN.md.
Начата работа в `b-ui`. После уточнения пользователя текущая проверка и поставка — B0 и B1.
Во время работы извне появились коммиты `f5b4be8` и `a035835` (оба B0), включающие
также ранние заготовки B2–B4. Они сохранены; текущие коммиты не переписывают эту историю.

## 2026-09-23 — B0, затем B1

Поручение пользователя:

> Я участник B. Прочитай AGENTS.md и docs/TEAM_PLAN.md целиком: мой трек — раздел 8,
> контракт между треками — раздел 4, владение файлами — раздел 3.
>
> Работаем только в b-ui; коммиты и push только туда, main — на синхронизациях.
> Править только файлы B. Не запускать run.py и не коммитить out/.
> gid в JSON, Plotly, pyvis и XLSX — только строкой.
> Один коммит на задачу, суффикс [codex], записывать промпты в этот файл.
>
> 1. Окружение: python -m venv .venv, активировать, pip install -r requirements.txt.
> 2. B0: tools/make_stub_outputs.py по §8.2, все файлы §4.1–4.4 в out_stub/;
>    запустить и проверить.
> 3. Коммит feat(stub): contract stub outputs [codex], push b-ui.
> 4. Сразу B1: app.py и ui/ по §8.2; проверить streamlit run app.py.

Дополнительное поручение:

> Можешь ли ты посмотреть на самом гитхаб репозитории и обновить.

### Проверенная база и решения B0

- GitHub проверен через подключённый GitHub: `main` на `cad60cd`, только данные и starter.py;
  ветки a-core и файлов A0 (requirements.txt, .gitignore, graf/load.py) пока нет.
- b-ui обновлена fast-forward до этой базы; main не изменялась.
- Создана `.venv` на Python 3.12. Зависимости взяты без изменений из §2 TEAM_PLAN
  во временный requirements-файл; общий requirements.txt не создавался.
- Временные `.venv/`, `out_stub/` и кэши исключены локально через `.git/info/exclude`.
  Участнику A нужно добавить общий `.gitignore` по A0.
- Загрузчик использует `graf.load.load`, когда он доступен; до A0 читает три parquet напрямую.
  Общие read-only helpers `ui/data.py` и подписи `ui/theme.py` входят в B0, чтобы
  генератор и B1 использовали один контракт, без копирования схем.
- Заглушка явно помечена в evidence, graph.json и run_meta.json. В топе только узлы,
  для которых найдены три реальных хронологических пути; скоры по-прежнему демонстрационные.
  `path_days` хранит день каждого узла; дата исходного seed равна дате первого перевода.
- Суммы/степени/связи взяты из parquet; роли/денежный след/устойчивость — моки seed=42.
- HTTPS Git не авторизован; публикация выполняется через подключённый GitHub без изменения main.

### Проверка B0

- `.venv/bin/python tools/make_stub_outputs.py --data data --out out_stub`: 5.16 с.
- 13 файлов контракта, 2248 узлов, 3119 рёбер, 4840 исходных транзакций, 20 кластеров, топ-30.
- 90 путей: по 3 к каждому узлу топа; все шаги существуют в исходных транзакциях,
  даты не убывают и задержка не превышает 7 дней.
- `.venv/bin/python -m pytest -q tests/test_ui_stub.py`: 4 passed.
- В Git не добавляются out_stub/, .venv/ или out/.

## 2026-09-23 — синхронизация с A и проверка B1

Уточнение пользователя:

> Сравнивай с a-core ветвью и делай на её основе, а в самом конце мы запушим
> в мейн общее решение.

- На повторной проверке появился A0 в main (`dbc69dc`) и A1 в a-core (`eb906662`).
  Выполнено слияние **a-core → b-ui**, коммит `9b64d3f`; файлы Б при конфликтах сохранены.
  В main ничего не отправлялось. `out/` пришла из уже существующих коммитов A;
  B не запускал run.py, не генерировал и не редактировал эти выгрузки.
- После синхронизации выполнено `.venv/bin/python -m pip install -r requirements.txt`.
  Заглушка повторно собрана уже через реальный `graf.load.load`.
- `graf.flow.chrono_reach` в A1 существует, но ещё бросает NotImplementedError.
  Обработан fallback на колонки Δ=2/31 с пометкой «без пересчёта A2».
- B1: кэш загрузки в ui/data.py, поиск по префиксу и полному gid, выбор строки топа
  с переходом к карточке, обработка изолятов, отсутствие CDN у pyvis, русские подписи.
- В реальном браузере проверен переход из топа в карточку и направленный ego-граф.
  Сервер запущен из `.venv`: `streamlit run app.py --server.address 127.0.0.1 --server.port 8501`.
- `python -m pytest -q tests/test_ui_stub.py tests/test_ui_app.py`: **13 passed** после слияния.
- Read-only проверка A1 out/: 2248 узлов, топ-30, 3119 рёбер; топ и карточка — без исключений.
  Ожидаются от A: seeds_review.csv, paths.csv, resilience.csv, ablation_links.csv,
  tracked_by_depth.csv, data_requests.csv, audit.csv. Интерфейс перечисляет отсутствующие файлы.
- README дополнен запуском B0/B1, статусом интеграции, ограничениями и командами тестов.
