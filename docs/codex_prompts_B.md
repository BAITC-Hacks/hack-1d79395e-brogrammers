# Работа участника B с Codex

## 2026-09-23 — исходное поручение

> Я участник Б, начинаем работу по плану участника Б (документы в папке).

Прочитаны `AGENTS.md` и `docs/TEAM_PLAN.md` целиком. Актуален TEAM_PLAN, а не ранний PLAN.md.
Начата работа в `b-ui`. После уточнения пользователя текущая проверка и поставка — B0 и B1.
Во время работы извне появились коммиты `f5b4be8` и `a035835` (оба B0), включающие
также ранние заготовки B2–B4. Они сохранены; текущие коммиты не переписывают эту историю.

## 2026-09-23 — B0, затем B1

Поручение пользователя:

> Я участник B. Прочитай AGENTS.md и docs/TEAM_PLAN.md целиком: мой трек — раздел 8,
> контракт между треками — раздел 4, владение файлами — раздел 3.
>
> Работаем только в b-ui; коммиты и push только туда, main — на синхронизациях.
> Править только файлы B. Не запускать run.py и не коммитить out/.
> gid в JSON, Plotly, pyvis и XLSX — только строкой.
> Один коммит на задачу, суффикс [codex], записывать промпты в этот файл.
>
> 1. Окружение: python -m venv .venv, активировать, pip install -r requirements.txt.
> 2. B0: tools/make_stub_outputs.py по §8.2, все файлы §4.1–4.4 в out_stub/;
>    запустить и проверить.
> 3. Коммит feat(stub): contract stub outputs [codex], push b-ui.
> 4. Сразу B1: app.py и ui/ по §8.2; проверить streamlit run app.py.

Дополнительное поручение:

> Можешь ли ты посмотреть на самом гитхаб репозитории и обновить.

### Проверенная база и решения B0

- GitHub проверен через подключённый GitHub: `main` на `cad60cd`, только данные и starter.py;
  ветки a-core и файлов A0 (requirements.txt, .gitignore, graf/load.py) пока нет.
- b-ui обновлена fast-forward до этой базы; main не изменялась.
- Создана `.venv` на Python 3.12. Зависимости взяты без изменений из §2 TEAM_PLAN
  во временный requirements-файл; общий requirements.txt не создавался.
- Временные `.venv/`, `out_stub/` и кэши исключены локально через `.git/info/exclude`.
  Участнику A нужно добавить общий `.gitignore` по A0.
- Загрузчик использует `graf.load.load`, когда он доступен; до A0 читает три parquet напрямую.
  Общие read-only helpers `ui/data.py` и подписи `ui/theme.py` входят в B0, чтобы
  генератор и B1 использовали один контракт, без копирования схем.
- Заглушка явно помечена в evidence, graph.json и run_meta.json. В топе только узлы,
  для которых найдены три реальных хронологических пути; скоры по-прежнему демонстрационные.
  `path_days` хранит день каждого узла; дата исходного seed равна дате первого перевода.
- Суммы/степени/связи взяты из parquet; роли/денежный след/устойчивость — моки seed=42.
- HTTPS Git не авторизован; публикация выполняется через подключённый GitHub без изменения main.

### Проверка B0

- `.venv/bin/python tools/make_stub_outputs.py --data data --out out_stub`: 5.16 с.
- 13 файлов контракта, 2248 узлов, 3119 рёбер, 4840 исходных транзакций, 20 кластеров, топ-30.
- 90 путей: по 3 к каждому узлу топа; все шаги существуют в исходных транзакциях,
  даты не убывают и задержка не превышает 7 дней.
- `.venv/bin/python -m pytest -q tests/test_ui_stub.py`: 4 passed.
- В Git не добавляются out_stub/, .venv/ или out/.

## 2026-09-23 — синхронизация с A и проверка B1

Уточнение пользователя:

> Сравнивай с a-core ветвью и делай на её основе, а в самом конце мы запушим
> в мейн общее решение.

- На повторной проверке появился A0 в main (`dbc69dc`) и A1 в a-core (`eb906662`).
  Выполнено слияние **a-core → b-ui**, коммит `9b64d3f`; файлы Б при конфликтах сохранены.
  В main ничего не отправлялось. `out/` пришла из уже существующих коммитов A;
  B не запускал run.py, не генерировал и не редактировал эти выгрузки.
- После синхронизации выполнено `.venv/bin/python -m pip install -r requirements.txt`.
  Заглушка повторно собрана уже через реальный `graf.load.load`.
- `graf.flow.chrono_reach` в A1 существует, но ещё бросает NotImplementedError.
  Обработан fallback на колонки Δ=2/31 с пометкой «без пересчёта A2».
- B1: кэш загрузки в ui/data.py, поиск по префиксу и полному gid, выбор строки топа
  с переходом к карточке, обработка изолятов, отсутствие CDN у pyvis, русские подписи.
- В реальном браузере проверен переход из топа в карточку и направленный ego-граф.
  Сервер запущен из `.venv`: `streamlit run app.py --server.address 127.0.0.1 --server.port 8501`.
- `python -m pytest -q tests/test_ui_stub.py tests/test_ui_app.py`: **13 passed** после слияния.
- Read-only проверка A1 out/: 2248 узлов, топ-30, 3119 рёбер; топ и карточка — без исключений.
  Ожидаются от A: seeds_review.csv, paths.csv, resilience.csv, ablation_links.csv,
  tracked_by_depth.csv, data_requests.csv, audit.csv. Интерфейс перечисляет отсутствующие файлы.
- README дополнен запуском B0/B1, статусом интеграции, ограничениями и командами тестов.

## 2026-09-23 — автономное завершение B2–B6

Поручение пользователя из приложенного текста и docs/CODEX_B.md:
работать автономно B0→B1→B2→B3→B4→B5→B-ready-for-final; B6 только после «ФИНАЛ в main».
Перед/после этапа читать a-core и STATUS_A, сливать новые коммиты при чистых своих изменениях.
Конфликты решать по владению; не запускать run.py с out/, не редактировать ядро A.
После этапа тесты B, 15-секундный smoke Streamlit, STATUS_B, отдельный коммит [codex] и push b-ui.
Не ждать A: использовать заглушку, отсутствующие значения показывать как «нет данных».
Ползунок Δ до явной готовности chrono_reach — 2/31. Все gid в UI/JSON/XLSX строкой.
README — цифры только реального out/; при незавершённом A4 — плейсхолдеры, после merge обновлять.
Скриншоты на реальных данных, демо-примеры из node_review.md. AI тестировать моками без ключа.
Не спрашивать подтверждение шагов. При отсутствии финала A: «B готов, жду финал A».

### B2

Задача: принять карту по коленам/сетевую раскладку, подсветку выбранного узла и соседей,
два слоя анализа, безопасный fallback Δ и устойчивость к неполным колонкам A.
На старте a-core = eb906662, STATUS_A ещё не опубликован; используем запасной вариант.

### Ускорение и срок
Пользователь: закончить за 3 часа, предложить способы ускорения. Решение: приоритет обязательных результатов, пакетные независимые проверки, без установки лишних плагинов. B2: 16 тестов; готовность API A включается только по STATUS_A; отдельный smoke-сервер автоматически останавливается.

### B3 — доказательная база
Выполнить B3 по TEAM_PLAN §8.2. Проверить 6 листов, API build_evidence/build_node_evidence, строковые gid, реальные основания переводов, literal Excel text. Результат: 20 тестов зелёные, включая реальный A2; out/ не изменён.

### B4 — кластеры, устойчивость, ассистент
Проверить страницы кластеров/устойчивости и read-only Responses tool loop: строгие схемы, направление связей, лимиты, unknown gid, повторение инструкции, previous_response_id и function_call_output. 30 тестов, ключ не требуется.
### B5 — README и совместимость
Написать README по 13 пунктам TEAM_PLAN, сохранить реальные скриншоты карты и доказательств, схему Mermaid. Пока A4 не влит — плейсхолдеры; добавить обновление из out/. По STATUS_A исправлены переносимость Windows-пути, UTF-8 и автоматическое создание out_stub для тестов. Проверено 32 теста, Streamlit smoke 15 секунд.
### Финальная стыковка B с A4/A5
Влить опубликованные A4/A5 после фиксации B5. Заполнить README только по out/ A, сверить формулу приоритета, добавить демо-примеры из node_review.md. Обновить реальные скриншоты. Результат: 49 тестов всего, graf.check OK, XLSX создаётся через интерфейс, smoke 15 секунд. Отметить B-ready-for-final; B6 запускать только после «ФИНАЛ в main».
### Последний пересчёт A6
Влить checkpoint 557a13c после публикации B-ready-for-final. CSV идентичны A5; изменились run_meta и XLSX. Обновить время в README из свежих метаданных и повторить тесты экспортера. B6 ожидает «ФИНАЛ в main».

### Коммуникация веток через forA.md / forB.md
Пользователь: «Сделай в ветке файл под названием forA.md для коммуникации с А Агентом, после коммита проверяй его ветку и читай его файл с названием forB.md, ЭТО ДЛЯ КОММУНИКАЦИИ ВЕТОК A и B между собой».
Создать forA.md в b-ui, передать готовность B0–B5, исправления замечаний A и условие запуска B6. После коммита и публикации проверить актуальную a-core и прочитать forB.md; далее повторять чтение после каждого собственного коммита в ходе работы.

### B6 — приёмка финала по A-002
Пользователь: «Ветка готова, ознакомься с forB». Прочитан A-002 в a-core: финальный main/v1.0 d71c067217a245be9e7f75644fbe0e3eaff5fd72. Выполнить ранее порученный B6: отдельный чистый клон, новый venv, точная команда README, время, CSV, XLSX и интерфейс без API-ключа. Ответить A через forA.md и обновить STATUS_B.
Из-за отсутствия HTTPS/SSH-авторизации получены SHA-проверенные Git-объекты через GitHub API, создано временное shallow-зеркало и отдельный клон --no-local. В новом venv: команда README 71,03 с с установкой, пайплайн 27,2789 с, 49 тестов, CHECK OK, 1567 текстовых gid в XLSX, реальный AppTest и smoke прошли. Рабочий out не пересчитывался, main и тег не менялись. Отдельный агент независимо сверил README и requirements без редактирования файлов.

### Приёмка A-003 и аудит всего проекта

Пользователь: «прочти forB.md». Прочитан свежий a-core 7d6aea6: A-003 принимает B-002 и закрывает B6; новых поручений нет.
Пользователь: «Прочти проект, вся актуальна ли, проверь всё ли выполнено по плану с веток А и Б».
Проверить актуальные main/a-core/b-ui по GitHub, весь TEAM_PLAN и исходный ТЗ, реализации обоих треков, контрактные выгрузки, тесты, документацию и сценарий демо. Независимые read-only подзадачи агентам: ядро A, реализация B, документация/ТЗ. Не менять исходники или out в рамках аудита.
Сверены деревья трёх веток: код и out совпадают, различаются только отчёты/промты. Повторно прошли 49 тестов, check копии out и 15-секундный smoke. Проверены строки gid, баланс атрибуции, реальные пути, формула приоритета, Δ=2 и полнота XLSX. Найдены три остатка B2/B3 и устаревшие документы/сценарии. Зафиксировать факты, строки и владельцев в forA.md (B-003), уточнить STATUS_B. Один коммит отчёта в b-ui; после публикации прочитать свежий forB.md. Функциональных исправлений и изменения релиза этот аудит не делает.

### A-004: понятность и основной функционал

Пользователь: «прочти forB.md» — прочитан A-004. Затем: мало времени, главное полезность, понятность и основной функционал ТЗ; «дай решения, пока ничего не делай сам» — работа остановлена после чтения, предложен приоритет основного сценария. После явных «делай» выполнить план в треке B без косметической переработки и изменения модели ядра.
По повторному «прочти forB.md» в процессе проверен A-005 / 2a70753; принят fast-forward. Исправить карту (visibility, клик, скрытый выбор), ego, полный XLSX и критерии, fallback2/31 и ссылки ассистента. Карточка: основания → ограничения → следующий запрос → связи → формальные критерии. README обновить по out A; зависимости сохранить в tools/constraints-py312.txt. requirements и общие документы A не менять.
Результат: 60 тестов; контрольный XLSX 210 запросов / 6 листов / 1742 текстовых gid; браузерные поиск/ego/карта и smoke 15 с. Отправить B-004 через forA.md; A пересоздаёт общий out и обновляет свои документы. Один коммит исправлений [codex] в b-ui, после публикации снова прочитать forB.md. Субагенты не привлекались.

После публикации 13500e9 и повторного чтения forB по поручению пользователя получен A-006 из f246625: A сообщает о разрешении на общую доработку. Передать уже готовый пакет и результаты в B-005, прекратить новые функциональные правки B для предотвращения конфликтов. Отдельный коммит коммуникации; основной пакет остаётся 13500e9.
