# Статус участника B

Ветка: `b-ui`. База A: `a-core` / `2a70753` (A-005). Проверен main / v1.0: `d71c067217a245be9e7f75644fbe0e3eaff5fd72`; текущая поставка исправляет замечания после v1.0.

B-ready-for-final

B6-complete — проверка v1.0 принята A в A-003. Замечания B из A-004 исправлены; ответ B-004 в forA.md. A должен пересоздать закоммиченный out/evidence.xlsx и проверить объединённую сборку перед новым финалом.

Передача по A-006: готовый функциональный пакет **13500e9** (60 passed за 12,93 с) передан A в B-005. A ведёт общую доработку; новые функциональные изменения B остановлены во избежание конфликтов.

| Задача | Статус | Коммит |
|---|---|---|
| B0 — заглушка контракта | готово | f455cca |
| B1 — каркас, топ, карточка, ego | готово | e4addfe |
| B2 — карта и два слоя | готово; замечания карты, ego и fallback исправлены | 9f04060; B-004 |
| B3 — XLSX | готово; все запросы, точные критерии, строковые gid | 8888258; B-004 |
| B4 — кластеры, устойчивость, AI | готово | bb08339 |
| B5 — README, схема, скриншоты | готово | f5dd795; числа и скриншоты обновлены после merge A5 |
| B6 — чистая машина после финала A | готово | отчёт B6 в этом коммите; проверен main d71c067 |

## Готово для A

- B0–B5 готовы. Полный pytest: 49 passed (32 B + 17 ядра). graf.check: CHECK OK, 2248 узлов, 107 кластеров, топ-30; все 12 проверок аудита успешны.
- Все вкладки проверены без API-ключа; Responses API — фейковым клиентом, без платных запросов. Streamlit smoke: 15 секунд, health 200, сервер проверки остановлен.
- graf.evidence_xlsx.build_evidence готов. Все 6 листов, API записи файла и байтов выбранного узла проверены; gid — текст Excel.
- Все gid в браузерных форматах передаются строками, исходные данные/выгрузки A не меняются.
- B6: отдельный чистый клон точного финального SHA, новый venv Python 3.12.14. Команда README завершилась за 71,03 с с установкой; пайплайн — 27,2789 с без пропусков.
- B6: 49 passed; CHECK OK; 6 листов XLSX и 1567 текстовых gid; AppTest на реальном out без API-ключа, денежный слой Δ=2, навигация и XLSX-кнопка прошли; health smoke 15 секунд прошёл.

## Запросы к A

- Готовность chrono_reach распознаётся и с сигнатурой функции. Ползунок 1–31 подключён по статусу A.
- A4/A5 влиты; README заполнен реальными результатами и примерами из node_review.md. Скриншоты обновлены на out/ A5. Ни один файл out/ не пересчитывался B.
- Пересчёт A6 влит; CSV не изменились относительно A5. README обновлён по последнему run_meta.json; новый evidence.xlsx проверен чтением и тестами B3.
- Исправлен Windows-тест пути (Path.name), временный статус пишется в UTF-8. B-тесты автоматически создают out_stub, если его нет.
- Windows-проверку A подтвердил в A-001: 49 passed, CHECK OK, smoke успешен.
- A-002 обработан; B6 завершён. Просьба отметить B6 закрытым по B-002. Исправления исходников и повторная публикация релиза не требуются.

## Решения, которые я принял сам

- При отсутствии готовности A1 в статусе A приложение по умолчанию использует out_stub;
  папку out можно явно выбрать для read-only проверки.
- Не ждать необязательных колонок: отображать «нет данных», а не придумывать признаки.
- README содержит запуск, формулы, ограничения и схему. tools/update_readme_results.py обновляет цифры только при готовом A4; до него плейсхолдеры.
- Скриншоты сделаны на реальном A5. Неполные дополнительные CSV дают сообщение, а не падение всего приложения.
- Git HTTPS не авторизован; подключённый GitHub API позволяет публиковать b-ui без изменения main.
  Объекты Git при синхронизации проверяются по SHA; опубликованная история не переписывается.
- В B6 транспорт заменён на SHA-проверенное зеркало GitHub API и отдельный `git clone --no-local`: терминальный HTTPS/SSH не авторизован. Это ограничение окружения отражено в forA.md; все проверки кода выполнены в новом venv.

## Блокеры

- Блокеров задач B нет. HTTPS/SSH-авторизацию Git в терминале пользователя B6 не подтверждает; main и тег не изменены.

## Детальный аудит после приёмки B6

Запрос пользователя: проверить актуальность всего проекта и выполнение плана A/B.
На срезе main d71c067, a-core 7d6aea6 и b-ui 1ee1a9e код и out идентичны. Повторно: 49 passed за 7,04 с; CHECK OK; Streamlit health 200/15 с; XLSX 6 листов и 1567 текстовых gid.

Открыты три замечания соответствия плану: рамки неполной наблюдаемости на Plotly-карте; немедленная подсветка соседей при клике; включение всех 210 запросов данных вместо 35 в общей XLSX-книге. Обязательные CSV и расчёты ядра корректны на текущих данных. Это остатки B2/B3, хотя задачи ранее формально приняты.
README/PROGRESS и документы в main ещё ожидают B6; старые сценарии демо содержат неверные проценты/ранги. Подробные места и владельцы — в forA.md, B-003. В рамках аудита код и out не менялись.

## B-004 — исправления основного сценария

Предыдущий раздел описывает состояние до исправлений. Сейчас карта различает visibility, немедленно выделяет соседей при клике и не приглушает все узлы при скрытом выборе. Ego топ-1: 33 узла, 0 пересечений кругов, контрастные рамки; направления сохранены. Без транзакций работают сохранённые Δ2/31. Карточка начинает с основания, контр-сигналов и следующего запроса; таблицы имеют русские подписи и даты без времени. Точные ворота и формулы общие для карточки и XLSX.

Контрольная общая XLSX из реального out: 210/210 запросов, 6 листов, 1742 текстовых gid с @. Книга узла содержит только его запросы. Локальный пример out_stub/evidence_review.xlsx не коммитится. Ассистент проверяет ссылки hypothesis по полному составу кластера; неизвестные ссылки по-прежнему маркируются.

Проверки: **60 passed**, Streamlit **health 200 / 15 с**; браузерные поиск/карточка/ego/клик карты прошли. README обновлён по A-005, зависимости зафиксированы в tools/constraints-py312.txt (Python3.12.14); pip check и установка --dry-run с constraints успешны. Современный sizing имеет fallback для старого Streamlit.

Осталось A: пересоздать out/evidence.xlsx, обновить общие docs/PROGRESS.md и docs/AUDIT.md по B-004, затем собрать релиз в согласованной точке. Рабочий out B не пересчитывал; main и v1.0 не менялись.
