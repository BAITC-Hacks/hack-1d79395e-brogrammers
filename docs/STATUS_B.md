# Статус участника B

Ветка: `b-ui`. База A: `a-core` / A1 `eb906662`. В main пишет A после финальной синхронизации.

| Задача | Статус | Коммит |
|---|---|---|
| B0 — заглушка контракта | готово | f455cca |
| B1 — каркас, топ, карточка, ego | готово | e4addfe |
| B2 — карта и два слоя | готово | текущий коммит B2 |
| B3 — XLSX | в работе | — |
| B4 — кластеры, устойчивость, AI | не начато | — |
| B5 — README, схема, скриншоты | не начато | — |
| B6 — чистая машина после финала A | не начато | — |

## Готово для A

- B0–B2 готовы; 16 тестов прошли, запуск Streamlit проверен 15 секунд. A1 out/ прочитан без ошибок.
- Все gid в браузерных форматах передаются строками, исходные данные/выгрузки A не меняются.

## Запросы к A

- Публиковать `docs/STATUS_A.md`, включая готовность A1 и строку «graf.flow.chrono_reach готов» после A2.
- После A2–A4 нужны paths, seeds_review, resilience, ablation_links, tracked_by_depth, data_requests, audit по контракту.

## Решения, которые я принял сам

- При отсутствии готовности A1 в статусе A приложение по умолчанию использует out_stub;
  папку out можно явно выбрать для read-only проверки.
- Не ждать необязательных колонок: отображать «нет данных», а не придумывать признаки.
- Текущие заготовки B2–B4 проходят приёмку поэтапно; наличие файла не означает завершённую задачу.
- Git HTTPS не авторизован; подключённый GitHub API позволяет публиковать b-ui без изменения main.
  Объекты Git при синхронизации проверяются по SHA; опубликованная история не переписывается.

## Блокеры

- B6 ждёт отметку A «ФИНАЛ в main». Других блокеров работы B нет.
