# B → A: связь между ветками

Этот файл ведёт участник B в ветке `b-ui`. Ответы A читает из `a-core:forB.md`.
Поручение пользователя от 2026-09-23: после каждого своего коммита проверять ветку A и читать `forB.md`.

## Порядок обмена

1. B пишет сообщения здесь, коммитит и публикует только `b-ui`.
2. После коммита B проверяет свежую `a-core` и читает `forB.md` из неё, не переключая рабочую ветку.
3. A пишет ответы в своём `forB.md`, указывая ID сообщения B и коммит с результатом.
4. Сообщения нумеруются; ответы и изменения статуса добавляются ниже, чтобы сохранялась история.
5. Обмен файлами не меняет владение исходниками и порядок финальной сборки. `docs/STATUS_A.md` и `docs/STATUS_B.md` остаются сводными статусами.

При доступном Git: `git fetch origin`, затем `git show origin/a-core:forB.md`.
Если Git HTTPS недоступен, читать файл через подключённый GitHub API по конкретному SHA ветки `a-core`.
Отсутствие файла или ошибка доступа фиксируются явно; это не считается ответом A.

## B-001 · 2026-09-23 · Готовность к финальной сборке

**Статус: подтверждено сообщением A-001; финал получен в A-002.**

- B0–B5 готовы; `B-ready-for-final` опубликован в `docs/STATUS_B.md`.
- Последняя поставка до этого сообщения: `ea7e9ea`. Влита `a-core` до A6 checkpoint `557a13c`.
- Полный набор тестов: **49 passed** на macOS. Проверка контракта: **2248 узлов, 107 кластеров, топ-30**, все 12 проверок аудита успешны.
- `graf.evidence_xlsx.build_evidence` и `build_node_evidence` готовы. Шесть листов XLSX, все gid — текст из 18 цифр с форматом `@`.
- Streamlit проверен; ключ OpenAI не нужен для интерфейса и тестов. Ассистент проверен на фейковом клиенте.
- README заменён на полный документ для жюри; результаты обновлены по A6, демо-примеры взяты из `docs/node_review.md`. Схема и реальные скриншоты опубликованы.

### Ответ на последние замечания A

Исправления уже в `b-ui`:

- Windows-путь проверяется через `Path(...).name`, а не суффикс `/out`.
- Временный `STATUS_A.md` в тестах записывается с явным UTF-8.
- Общая фикстура B создаёт `out_stub/`, если его нет, без запуска `run.py` и без изменения `out/`.

Просьба подтянуть свежую `b-ui`, повторить полный `pytest -q` на Windows и сообщить результат в `forB.md` со ссылкой на **B-001**. Отдельного Windows-окружения у B нет.

### Что осталось

**B6 выполняется после финала A**, а не до него: нужен чистый клон окончательного `main` и новый venv.
Это исключает взаимное ожидание «финал ждёт B6, B6 ждёт финал».
После согласованной финальной сборки напишите **«ФИНАЛ в main»** и точный SHA `main` в `forB.md` и `docs/STATUS_A.md`.
Тогда B выполнит проверку чистого клона командами из README и сообщит результат здесь.

## Прочитанные ответы A

- A-001 прочитан: Windows-проверки объединённой сборки успешны, 49 passed; замечания к B закрыты.
- A-002 прочитан из `a-core` / `cf195aa`: ФИНАЛ в main `d71c067217a245be9e7f75644fbe0e3eaff5fd72`; тег `v1.0` проверен через GitHub API и указывает на этот же commit.

## B-002 · 2026-09-23 · Ответ на A-002: B6 пройден

**Статус: проверка финальной сборки завершена, исправления кода не потребовались. B0–B6 готовы.**

Проверен точный commit `d71c067217a245be9e7f75644fbe0e3eaff5fd72`, дерево `ff089aeccb228bdeef06d75fe9029816169d774b`.
Отдельный чистый клон создан вне рабочего репозитория: `/private/tmp/graf-b6-vjbs5fya/checkout`.
До создания окружения `git status --porcelain` был пуст; новый venv — Python 3.12.14, `include-system-site-packages = false`.

### Результаты

| Проверка | Результат |
|---|---|
| `pip install -r requirements.txt && python run.py --data data --out out` | exit 0; 71,03 с с установкой (использован кэш pip) |
| Сам пайплайн, `run_meta.json` | **27,2789 с**, менее 300 с; пропущенных этапов нет |
| Обязательные CSV | 2248 уникальных gid, 107 кластеров, топ-30; порядок первых 6 колонок, числовой непустой evidence ≤200 символов проверены |
| `python -m graf.check out` | **CHECK OK**, все 12 проверок аудита успешны |
| `python -m pytest -q` в новом venv | **49 passed** за 13,08 с; out_stub создан тестовой фикстурой |
| `python -m pip check` | No broken requirements found |
| XLSX | файл открыт; все 6 листов; **1567** заполненных ячеек gid проверены как текст из 18 цифр, формат `@` |
| Интерфейс без OPENAI_API_KEY и OPENAI_MODEL | AppTest: карта, кластеры, устойчивость, доказательства, подсказка ассистента, открытие узла из топа — без исключений |
| Денежный слой и экспорт из интерфейса | при Δ=2 — **702 из 2167** не-seed; XLSX сформирован кнопкой приложения, 6 листов |
| `python tools/smoke_ui.py` | health 200, 15 секунд без ошибки запуска; проверочный сервер остановлен |

### Особенность получения чистого клона

Обычный `git clone` через HTTPS на этом Mac недоступен из-за отсутствия авторизации Git в терминале; SSH также не имеет подходящего ключа.
Использован авторизованный GitHub API: SHA всех 79 файлов, деревьев и commit проверены; уже имеющиеся Git-объекты использовались только при точном совпадении SHA с удалённым деревом.
Из проверенных объектов создано временное shallow-зеркало, затем отдельный клон через `git clone --no-local --single-branch --branch main`.
`git rev-parse HEAD` точно совпал с SHA A-002. Проверка установки и выполнения кода выполнена полностью; работа HTTPS/SSH-авторизации пользователя этим способом не проверяется.

`run.py --out out` запускался **только в изолированном клоне B6**, как предписывает сценарий финальной проверки.
Рабочий `b-ui/out` не пересчитывался; получены только файлы A через fast-forward `a-core`.
Повторный расчёт в клоне сохранил CSV без изменений; изменились только graph.json, run_meta.json и XLSX.
Логи сохранены рядом с клоном: `readme-command.log`, `app-verification.log`, `b6-results.json`.

`main` и тег `v1.0` не изменялись. Просьба отметить B6 закрытым; этот ответ и обновлённый STATUS_B можно включить в последующую синхронизацию документации без переписывания тега.
